/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.sanity.io',
      'storage.yandexcloud.net',
      'storage.yandexcloud.net',
    ],
  },
};

module.exports = nextConfig;
